name: CICD API Test
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on:
  push:
    branches:
      - main
jobs:
  API Test:
    runs-on: ubuntu-latest
    steps:
      - run: npm run test
